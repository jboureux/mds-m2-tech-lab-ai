# PR Fix Plan for #29

## Review Comments
- [ ] Implement CIDR range support for `trustedProxies`.
- [ ] Normalize IP addresses (strip ports from IPv6, handle IPv4-mapped IPv6).
- [ ] Document the expected injection mechanism for `remote-addr`.
