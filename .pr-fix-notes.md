## PR Fix Summary

### CI
- ✅ quality — fixed: Added `prisma generate` step before tests to ensure Prisma Client is available.

### Review comments
- ✅ Sidebar UI — fixed: Replaced background color with PRP-compliant `#0F172A` and removed unneeded transparency.
- ✅ Dynamic Network Info — fixed: Removed hardcoded `networkLocation` override in `AppSidebarContent`.
- ✅ KPI Cards — fixed: Removed unnecessary trend statistics under the main values.
- ✅ Auth 500 Error — fixed: Enforced `Boolean` type for `emailVerified` and forced a full build/regeneration to clear runtime cache.

### Commits pushed
- `feat(ci): add prisma generate step to workflow`
- `fix(admin): refine UI/UX and fix dynamic data bugs`
- `refactor(admin): align design with PRP and clean up KPI cards`
